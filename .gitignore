node_modules/
dist/
.astro/

# Pipeline working directory. Regenerable from Overture, and pilot-raw.json is
# ~2MB of cached query result — it belongs on disk, not in git history.
# src/data/ IS committed: that is the published site's data.
data-work/

.DS_Store
*.log
.env
.env.production
.vercel
